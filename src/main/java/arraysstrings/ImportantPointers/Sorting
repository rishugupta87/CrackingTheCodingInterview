 public static void main(String[] args) {
     int[] nums = {10,20,30,19,1,3};
     char[] chars = {'i', 'r', 's', 'a'};
     Arrays.sort(nums);
     Arrays.sort(chars);
     System.out.println(Arrays.toString(nums)); // nums in sorted order
     System.out.println(Arrays.toString(chars); //characters in sorted order
 }

 To sort a string.
 private static String sort(String str) {
     char[] charArray = str.toCharArray();
     Arrays.sort(charArray);
     return new String(charArray); //return a new string since strings are immutable
 }

Arrays.sort internal implementation
Dual-Pivot Quicksort by Vladimir Yaroslavskiy, Jon Bentley, and Joshua Bloch.
This algorithm
     * offers O(n log(n)) performance on many data sets that cause other quicksorts to degrade to
     quadratic performance, and is typically faster than traditional (one-pivot) Quicksort implementations.
 ####### ####### ####### ####### ####### ####### ####### ####### ####### ####### ####### ####### #######