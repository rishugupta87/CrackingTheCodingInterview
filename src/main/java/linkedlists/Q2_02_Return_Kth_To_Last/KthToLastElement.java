package linkedlists.Q2_02_Return_Kth_To_Last;

import linkedlists.library.Node;

/**
 *  Implement an algorithm to find the kth to last element of a singly linked list.
 */
public class KthToLastElement {

    /**
     *
     * Finding Kth to the last element is equal to finding n - kth from the front where n is the size of the LL
     * If we do not know the size of LL we need to iterate once to find the size then iterate again to look for n-kth element
     *
     * Time = O(n) spze if O(1) this is easy to implement
     *
     * @param head
     * @return
     */
    public int kthToLast(Node head) {
        //easy to implement
        return 0;
    }
}
